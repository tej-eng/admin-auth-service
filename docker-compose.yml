services:
  admin-auth-service:
    build: .
    container_name: admin-auth-service
    restart: always
    env_file:
      - .env
    ports:
      - "8008:8008"

    networks:
      - dhwaniastro-net


networks:
  dhwaniastro-net:
    external: true
