services:

 admin-auth:
    build: .
    container_name: admin-auth-service
    ports:
      - "4001:4001"
    env_file:
      - .env
    networks:
      - dhwaniastro-net

networks:
  dhwaniastro-net:
    external: true



 